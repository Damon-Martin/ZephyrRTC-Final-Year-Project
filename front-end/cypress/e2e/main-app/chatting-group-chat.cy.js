/// <reference types="cypress" />

describe('Messaging Group Chat E2E Tests', () => {
  // Login to app
  beforeEach(() => {
    cy.visit('https://localhost/')
  });

  it('test-message-john-to-all', () => {
    assert.fail('Test Not Made yet')
  });

  it('test-recieve-message-from-members', () => {
    assert.fail('Test Not Made yet')
  });

  it('test-delete-msg-from-user', () => {
    assert.fail('Test Not Made yet')
  });

  it('test-delete-msg-from-other-user-makes-msg-invisable', () => {
    assert.fail('Test Not Made yet')
  });

  it('test-send-image', () => {
    assert.fail('Test Not Made yet')
  });

  it('test-send-document', () => {
    assert.fail('Test Not Made yet')
  });
});
