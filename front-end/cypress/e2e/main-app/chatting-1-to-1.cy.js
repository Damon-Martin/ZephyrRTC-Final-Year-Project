/// <reference types="cypress" />

describe('Messaging 1 to 1 E2E Tests', () => {
  // Login to app
  beforeEach(() => {
    cy.visit('https://localhost/')
  });

  it('test-message-john', () => {
    assert.fail('Test Not Made yet')
  });

  it('test-recieve-message-sarah-to-john', () => {
    assert.fail('Test Not Made yet')
  });

  it('test-delete-msg', () => {
    assert.fail('Test Not Made yet')
  });

  it('test-send-image', () => {
    assert.fail('Test Not Made yet')
  });

  it('test-send-document', () => {
    assert.fail('Test Not Made yet')
  });
});
