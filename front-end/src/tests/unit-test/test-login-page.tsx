const chai = require('chai');
const assert = chai.assert;

describe('LoginPage: Email Checker', () => {
  it('test-bad-email-gives-an-error-component-on-page', () => {
    assert.fail('Test Not Made Yet');
  })
})